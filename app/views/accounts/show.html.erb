<%= @account.inspect %>
<%= @account.transaction_on_account.first.value %>

<%= simple_form_for @actual_transaction=TransactionOnAccount.new, :url => account_transaction_on_accounts_path(@account, @actual_transaction) do |f| %>
 <h3>Add new Transaction</h3>

        <div class="form-group">
          <%= f.label :category %>
          <%= f.text_field :category, class: 'form-control', :required => true %>
        </div>
        <div class="form-group">
          <%= f.label :value %>
          <%= f.number_field :value, class: 'form-control', :step => 'any', :required => true %>
        </div>
        <div class="form-group">
          <%= f.label :reason %>
          <%= f.text_field :reason, class: 'form-control' %>
        </div>
        <div class="form-group">
          <%= f.label :date %>
          <%= f.text_field :date, class: 'dp', :data => {:provide => "datepicker"}, :required => true %>
        </div>

        <%= f.submit 'Add new transaction', :class => 'btn btn-success' %>
    <% end %>

<%= simple_form_for @actual_transaction=TransactionOnAccount.new, :url => account_transaction_on_accounts_path(@account, @actual_transaction) do |f| %>
 <h3>Add new Transaction</h3>

        <div class="form-group">
          <%= f.label :category %>
          <%= f.text_field :category, class: 'form-control', :required => true %>
        </div>
        <div class="form-group">
          <%= f.label :value %>
          <%= f.number_field :value, class: 'form-control', :step => 'any', :required => true %>
        </div>
        <div class="form-group">
          <%= f.label :reason %>
          <%= f.text_field :reason, class: 'form-control' %>
        </div>
        <div class="form-group">
          <%= f.label :date %>
          <%= f.text_field :date, class: 'dp', :data => {:provide => "datepicker"}, :required => true %>
        </div>

        <%= f.submit 'Add new transaction', :class => 'btn btn-success' %>
    <% end %>
